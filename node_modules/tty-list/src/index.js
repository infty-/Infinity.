var term = require('node-terminal');
var charm = require('node-charm');
var help = require('help-func');
help.global.globalize();
globalize(help.propConstr);
globalize(help.func, Function.prototype);
globalize(help.Func, Function);

var manager;

manager = Function.create(function Manager() {
    this.list = [];
}, undefined, {
    prototype: Member(Object.create(Object.prototype, {
	commit: function () {
	}
    }))
});

module.exports = manager;